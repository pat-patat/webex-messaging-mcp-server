# Webex MCP Server Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# AUTHENTICATION (choose one option)
# =============================================================================

# Option 1: Static API Token (simple, but expires every 12 hours)
# Get your token from: https://developer.webex.com
# Note: Tokens expire every 12 hours and need manual renewal
# WEBEX_PUBLIC_WORKSPACE_API_KEY=your-webex-api-token-here

# Option 2: OAuth Authentication (recommended for long-running use)
# Create an integration at: https://developer.webex.com/my-apps
# 1. Click "Create a New App" > "Create an Integration"
# 2. Set Redirect URI to: http://localhost:PORT/callback (any port works)
# 3. Select scopes (spark:all for full access, or choose specific scopes)
# 4. Copy Client ID and Client Secret below
WEBEX_OAUTH_CLIENT_ID=your-client-id
WEBEX_OAUTH_CLIENT_SECRET=your-client-secret
WEBEX_OAUTH_SCOPE=spark:all

# Note: If both static token and OAuth are configured, static token takes priority

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================

# Webex API Base URL (defaults to https://webexapis.com/v1)
# Only change this if you're using a different Webex environment
WEBEX_API_BASE_URL=https://webexapis.com/v1

# User Information (for reference)
WEBEX_USER_EMAIL=your-email@company.com

# Server Mode (defaults to STDIO)
# Set to 'http' for HTTP mode, 'sse' for SSE mode, or leave blank for STDIO
MCP_MODE=

# Port for HTTP/SSE mode (defaults to 3001)
# Only used when running with --http or --sse flag
PORT=3001
